{
  "name": "sg-nearby-bus-stops",
  "version": "1.0.0",
  "private": true,
  "description": "Find nearby Singapore bus stops on an interactive map.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "data": "node scripts/build-stops.cjs",
    "streets": "node scripts/build-streets.cjs",
    "malls": "node scripts/build-malls.cjs",
    "mrt": "node scripts/build-mrt.cjs",
    "places": "node scripts/build-places.cjs",
    "build": "npm run data && npm run streets && npm run malls && npm run mrt && npm run places && vite build",
    "build:pages": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
