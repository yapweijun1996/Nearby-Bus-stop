# Copy to .env and fill in your LTA DataMall API Account Key.
# Used by `npm run data` / `npm run build` to refresh public/bus-stops.jsonl.
# .env is gitignored — never commit your key.
LTA_ACCOUNT_KEY=your-account-key-here
